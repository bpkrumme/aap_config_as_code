all:
  children:
    aap:
      children:
        aapdb:
          hosts:
            postgres.mobile.bk.lab:
        automationhub:
          hosts:
            hub.mobile.bk.lab:
        controller:
          hosts:
            controller.mobile.bk.lab:
#        edaserver:
#          hosts:
#           eda.mobile.bk.lab:
    utility:
      hosts:
        utility.mobile.bk.lab:
    managed:
      hosts:
        node01.mobile.bk.lab:
        node02.mobile.bk.lab:
        node03.mobile.bk.lab:
    labhost:
      hosts:
        localhost:
          ansible_connection: local
    ungrouped: {}
