---
controller_credentials:
  - name: "HomeLab Machine"
    description: ""
    credential_type: "Machine"
    organization: "HomeLab"
    inputs:
      become_method: sudo
      become_username: root
      ssh_key_data: "{{ machine_ssh_private_key }}"
      username: ansible

...
